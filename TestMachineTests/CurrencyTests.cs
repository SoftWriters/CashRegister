using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BasicConsoleAppTests
{
    [TestClass]
    public class when_interating_over_decreasing_known_currency
    {
        /*
        [TestMethod]
        public void it_should_always_decrease()
        {
            ICurrency previousCurrency = null;
            foreach (var currency in Currency.KnownCurrency)
            {
                if (previousCurrency != null) Assert.IsTrue(previousCurrency.Value > currency.Value);
            }
        }
        */
    }
}
